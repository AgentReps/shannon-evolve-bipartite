__pycache__/
*.pyc
*.out
.DS_Store
STOP
# worktrees live outside the repo by default, but ignore if anyone puts
# them inside:
shannon-evolve-runs/
# runtime state written by step.py in solo mode:
log/state-*.json
log/elites/
log/branch-solo.md
# Python virtualenvs and tooling caches (never commit these):
.venv/
.pytest_cache/
# description/ is a reference copy — keep the reproduced figures on disk
# but don't stage them:
description/scratch/*.png
